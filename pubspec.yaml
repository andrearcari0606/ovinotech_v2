name: ovinotech_v2
description: Sistema de manejo de ovinos

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.2

  uuid: ^4.3.3

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # 🔥 HIVE (LOCAL)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # 🔥 FIREBASE (NUVEM)
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.15.0

  # 📊 GRÁFICOS
  fl_chart: ^0.66.0
 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

  # 🔥 HIVE GENERATOR (IMPORTANTE)
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/ecc/